// import ReactDOM from 'react-dom'

// export const Modal = ({ children }) => {
//   return ReactDOM.createPortal(<div style={{ position: 'absolute', top: 20, right: 20, bottom: 20, left: 20, background: 'yellow'}}>{children}</div>, document.getElementById('modal'))
// }